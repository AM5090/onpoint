const track=document.querySelector(".slide_track");let x1=null,x2=null,moveInterval=null;document.addEventListener("touchstart",t=>{t=t.touches[0];x1=t.clientX}),document.addEventListener("touchmove",t=>{t=t.touches[0];x2=t.clientX,moveInterval=Math.floor(x2-x1),trackPosition=Math.floor(Number(getComputedStyle(track).left.slice(0,-2))),console.log("moveInterval",moveInterval),console.log("trackPosition",trackPosition),moveInterval<trackPosition&&(track.style.left="-1024px")});